# Python bytecode
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
*.whl

# Build artefacts
# excel_cleaner.spec SÍ se versiona: fuente única del build PyInstaller
# (skill pyinstaller-packaging-workflow) y consumido por tests/test_branding.py.
build/
dist/
*.spec
!excel_cleaner.spec

# Virtual environments
.venv/
venv/
env/
.env/
venv.bak/

# OS/editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
*.sublime-merge
*.sublime-workspace
.vscode/
.idea/
*.bak
*.orig
*.tmp
*.log

# Windows
# (keep logs only if you explicitly need them in the repo; by default, ignore user logs)
logs/

# Freebuff client local state
.freebuff/

# Local demo artifacts (generated, not part of the stable engine)
demo_portfolio/

# Dependency snapshot (if you choose to keep it local only)
build-frozen.txt
